<?xml version="1.0" encoding="utf-8"?>
<!--Todo 1. (Answer) Tambahkan drawer layout dengan 2 child, (1)main content, (2)drawer content (done: activity_main.xml) -->
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainDrawer"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.labsgn.labsgn_learn_android.MainActivity">

        <include
            android:id="@+id/mainToolbar"
            layout="@layout/toolbar" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/mainToolbar"
            android:layout_margin="@dimen/default_margin"
            android:background="@color/colorPrimary"
            android:text="@string/activity1" />

    </RelativeLayout>
    <fragment
        android:id="@+id/mainFragment"
        android:layout_width="@dimen/drawer_width"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:layout="@layout/main_fragment"
        android:name="layout.MainFragment"
        tools:layout="@layout/main_fragment">

    </fragment>
</android.support.v4.widget.DrawerLayout>
